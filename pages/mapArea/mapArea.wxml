<!--mapArea.wxml-->
<view class="content_list">
  <view class="content_list_item" data-id="{{ item.id }}" data-title="{{ item.title }}" wx:for="{{ areaList }}" wx:key="id" bindtap="areaFn">{{ item.title }}</view>
</view>